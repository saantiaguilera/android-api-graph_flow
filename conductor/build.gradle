android {
    defaultConfig {
        minSdkVersion 16 // As per conductor asks
    }
}

dependencies {
    compile misc.conductor

    if (isForPublish) {
        compile me.core
    } else {
        compile project(path:':core')
    }
}